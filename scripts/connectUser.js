const connectUser = (socket) => {
  // socket.on('connected users', function (users) {
  //   const usersList = document.querySelector('.play__players-ul');

  //   usersList.innerHTML = '';

  //   users.forEach(user => {
  //     const liElement = document.createElement('li');
  //     liElement.classList.add('play__player');
  //     liElement.textContent = user;
  //     usersList.append(liElement);
  //   });
  // });
}

export { connectUser }
